<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.piaoniu.demo.dao.IndexDao">
    <select id="findShowtype" resultType="com.piaoniu.demo.pojo.ShowType">
        select show_type from pn_show_type;
    </select>
    <select id="findShowByAll" resultType="com.piaoniu.demo.model.IndexModel" parameterType="com.piaoniu.demo.pojo.Show">
        select a.show_name,a.show_time,a.show_picture,b.show_place,a.show_comment_grade,a.show_money,d.city_name from pn_show a,pn_city d,pn_show_place b where a.show_place_id=b.show_place_id and a.show_status_id=1 and a.city_id=d.city_id
            <if test= "city_id!=0"> and a.city_id= ${city_id}</if>
            <if test= "show_type_id!=0"> and a.show_type_id = ${show_type_id}</if>
            <if test= "show_times==1"> and YEARWEEK(date_format(a.show_time,'%Y-%m-%d')) = YEARWEEK(now())</if>
            <if test= "show_times==2"> and date_format(show_time,'%Y-%m')=date_format(now(),'%Y-%m')</if>
            <if test= "show_time!=null">and a.show_time = ${show_time}</if>
    </select>
</mapper>